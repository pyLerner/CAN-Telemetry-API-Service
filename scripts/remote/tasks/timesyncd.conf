# Пример конфигурации для systemd-timesyncd (редактируйте под свою сеть).
# Развёртывание: ./scripts/remote/remote-run.sh <target> ./scripts/remote/tasks/apply-timesyncd.sh

[Time]
NTP=0.ru.pool.ntp.org 1.ru.pool.ntp.org
FallbackNTP=time.google.com
